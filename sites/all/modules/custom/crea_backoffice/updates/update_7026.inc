<?php

// Activation du thème "atlas_bootstrap"
theme_enable(array('atlas_bootstrap'));

// Désactivation du thème "crea"
theme_disable(array('crea'));

// installation du module "panels_bootstrap_layout_builder"
module_enable(array('panels_bootstrap_layout_builder'));